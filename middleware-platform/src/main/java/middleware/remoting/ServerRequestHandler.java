package middleware.remoting;

import middleware.gateway.ServerContract;

/**
 * Server Request Handler — aceita conexão e entrega mensagem lógica ao Broker (Fase C).
 */
public interface ServerRequestHandler extends ServerContract {
}
