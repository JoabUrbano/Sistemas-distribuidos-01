package middleware.remoting.protocol;

import middleware.gateway.ServerContract;

/**
 * Protocol Plug-In no gateway: transporte TCP ou UDP (Fase F).
 */
public interface GatewayTransport extends ServerContract {
}
